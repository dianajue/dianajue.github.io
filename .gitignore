# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler, if gems are ever installed into the project rather than globally
vendor/bundle/
.bundle/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db
