# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Bundler, if gems are ever installed into the project rather than globally
vendor/bundle/
.bundle/

# Node
node_modules/

# Obsidian workspace settings (the repo root is opened as a vault for writing
# posts). Jekyll skips dotfiles, so this never reaches the site either way.
.obsidian/

# OS
.DS_Store
Thumbs.db
