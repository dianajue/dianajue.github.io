# Only needed to preview the site locally. GitHub Pages builds without it.
#   gem install bundler && bundle install && bundle exec jekyll serve
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"
